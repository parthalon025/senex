C# 11+/.NET 8+: nullable reference types enabled (`<Nullable>enable</Nullable>`); records for value-shaped types; pattern matching for state-shape decisions; `using` declarations for `IDisposable`/`IAsyncDisposable`. Common defects: `async void` on non-event-handler methods (exceptions cannot be awaited); `Task` returned but not awaited (silent failure); missing `ConfigureAwait(false)` in library code that may run in UI/SynchronizationContext; `Dispose` not called because of an exception path bypassing `using`; `IEnumerable<T>` materialized twice across an `await`; `null!` forgiveness used to silence the analyzer rather than fix the contract; `lock(this)` / `lock(typeof(...))` rather than a private `static readonly object`; cancellation tokens accepted but never checked; `IAsyncEnumerable` consumed with `ToList()` instead of `await foreach`.
